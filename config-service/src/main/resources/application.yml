server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/toledo96/microservicio2
          default-label: config-server
          search-paths: config-data
  application:
    name: config-service